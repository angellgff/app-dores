/**
 * Placeholder nativo (iOS/Android).
 * El polyfill de CSSStyleDeclaration solo es necesario en web.
 * Metro resuelve `import './polyfills'` a `polyfills.web.ts` en web
 * y a este archivo en iOS/Android.
 */
